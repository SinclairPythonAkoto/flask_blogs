version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "3304:5004"
    restart: always
  frontend:
    build: ./frontend
    ports:
      - "3303:5003"
    restart: always
    depends_on:
      - backend
